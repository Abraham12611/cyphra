server {
    listen 80;
    listen [::]:80;

    server_name sui.bnshub.org www.sui.bnshub.org; # Optional: include www if needed

    location / {
        proxy_pass http://34.244.133.61:8000; # Your application's address
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support (if your application needs it)
        # proxy_http_version 1.1;
        # proxy_set_header Upgrade $http_upgrade;
        # proxy_set_header Connection "upgrade";
    }

    # Optional: You can add specific locations for static files if Nginx should serve them directly
    # location /static/ {
    #     alias /path/to/your/app/static/;
    #     expires 30d;
    #     add_header Cache-Control "public";
    # }
}